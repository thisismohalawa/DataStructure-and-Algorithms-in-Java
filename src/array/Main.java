package array;

/*
 * Arrays : the most used data structure.
 * Static Array : fixed length container containing n elements
 * indexable from range [0,n-1], used to sort and access sequential data,
 * temporarily storing objects...
 *
 * Dynamic Array : can grow and shrink in size.
 * Creating Dynamic Array : create static array with an initial capacity, add element
 * and keep tracking number of elements, if adding another element will exceed the
 * capacity, then we double the capacity by creating another static array and
 * coping the original elements.
 *
 * */
public class Main {

    public static void main(String[] main) {



    }
}
